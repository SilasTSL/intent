<% layout('layouts/boilerplate')%>
<div id="profilePageContainer">
    <h1 class="profilePageTitle">Hi <%= user.username.charAt(0).toUpperCase() + user.username.slice(1) %>,</h1>
    <div class="changePasswordBody">
        <input type="text">
    </div>
</div>